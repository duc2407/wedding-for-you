import 'dart:ui';

// Màu nền pastel dịu nhẹ
const Color backgroundLightest = Color(0xFFF6F5F2); // nền sáng nhất
const Color backgroundLight = Color(0xFFF0EBE3); // nền nhạt hơn
const Color pinkLight = Color(0xFFF3D0D7); // hồng pastel
const Color pinkVeryLight = Color(0xFFFFEFEF); // hồng rất nhạt
const Color pinkDark = Color(0xFFC94F5D); // hồng đậm

// Màu chữ bình thường (dùng cho body text, paragraph)
const Color textNormal = Color(
  0xFF4A4A4A,
); // xám đen vừa phải, dễ đọc trên nền sáng

// Màu chữ đậm, nổi bật (dùng cho tiêu đề, heading, font bold)
const Color textBold = Color(
  0xFF1A1A1A,
); // gần như đen, rất nổi bật trên nền sáng
